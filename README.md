# Image Manager Web App
A simple Streamlit web app to upload and process images (grayscale, resize, blur, rotate, crop, analyze, and download).

## Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py
